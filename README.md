Author: Martin Anuonye

This is a mergesort program fit with parallelization. mergesort_par.cpp is currently configured
to import omp_tasking.hpp from tooling/, so accomodate or reconfigure import as necessary. Run make
to compile, and the number of random elements and number of threads to create should be given
as arguments on execution.